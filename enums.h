#ifndef ENUMS_H
#define ENUMS_H

enum Status
{
    Menu = 0,
    LearnMode = 1,
    TestMode = 2,
};

enum Constans
{
    MaxUsers = 5,
};

enum LastAction
{
    None = 0,
    Learn = 1,
    Test = 2,
    LearnTest = 3,
};

#endif // ENUMS_H
